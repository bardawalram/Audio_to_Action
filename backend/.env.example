# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True

# Database Configuration
DB_NAME=reatoa_db
DB_USER=postgres
DB_PASSWORD=your-password-here
DB_HOST=localhost
DB_PORT=5432

# Whisper Configuration (Local Model)
WHISPER_MODEL_SIZE=base
WHISPER_DEVICE=cpu
# Use 'cuda' for WHISPER_DEVICE if GPU available for faster processing

# Default Settings
DEFAULT_EXAM_TYPE=UNIT_TEST

# CORS (for production)
# CORS_ALLOWED_ORIGINS=http://localhost:5173
# ALLOWED_HOSTS=localhost,127.0.0.1
